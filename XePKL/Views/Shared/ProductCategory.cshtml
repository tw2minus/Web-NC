@model List<Modell.EF.DanhmucSP>


    <h5 class="m_1">Danh mục bán hàng</h5>
   
    <ul class="kids">
   @foreach (var item in Model.Where(x=>x.ParentID==null))
    {
        var child = Model.Where(x => x.ParentID == item.ID);
        var parentClass = child.Count() >0 ? "class=dropdown-toggle data-toggle=dropdown" :"";
        <li class="dropdown">
            <a href="/san-pham/@item.MetaTitle-@item.ID"@parentClass>@item.Name</a>
            @if (child.Count() > 0)
            {
                <ul class="dropdown-menu">
                @foreach (var childItem in child)
                {
                    <li><a href="/san-pham/@childItem.MetaTitle-@childItem.ID">@childItem.Name</a></li>
                }

                </ul>
            }

        </li>

    }
    </ul>
    <section class="sky-form">
        <h4>Danh mục 2</h4>
        <div class="row row1 scroll-pane">
            <ul class="kids">
                <li><a href="#">Xe Ducati</a></li>
                <li><a href="#">Xe Yamaha</a></li>
                <li><a href="#">Xe NakedBike</a></li>
                <li><a href="#">Phụ kiện xe</a></li>
                <li><a href="#">Quần áo</a></li>
                <li><a href="#">Mobile App</a></li>
            </ul>
        </div>
    </section>

    <script src="~/Assets/client/js/jquery.easydropdown.js"></script>
